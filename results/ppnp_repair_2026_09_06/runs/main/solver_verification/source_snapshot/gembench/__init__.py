"""GEM-Bench v0 — a small, explicit benchmark harness for genome-scale metabolic models.

Design rules (see roadmap WS0):
  * every dataset carries provenance and a licence note;
  * every protocol is a plain function with all thresholds as arguments;
  * every result is written with a benchmark card (dataset, model, protocol, leakage).
"""

__version__ = "0.0.1"
